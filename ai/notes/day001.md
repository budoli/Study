# Day 001 - 텍스트 파일 읽기

## 오늘의 목표

`pathlib.Path`와 `read_text()`를 사용해서 텍스트 파일을 읽는다.

## 배운 개념

- `Path`: 파일 경로를 다루는 객체
- `read_text(encoding="utf-8")`: 텍스트 파일을 문자열로 읽는 메서드
- `exists()`: 파일이 존재하는지 확인하는 메서드
